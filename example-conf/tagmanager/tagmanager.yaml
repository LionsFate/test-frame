# I typically set my options in ~/pg_service.conf for everything to use.
# If you want the full options on 1 line, it would look something like this -
#database: "user=frame password=frame host=localhost port=5433 dbname=frame pool_max_conns=10 pool_min_conns=0 pool_max_conn_lifetime=1h pool_max_conn_idle_time=5m pool_health_check_period=1m"

# The short version though, that I can put in all my configuration files, is this one -
database: "service=frame"

